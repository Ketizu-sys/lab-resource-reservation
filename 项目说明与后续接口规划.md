# 实验室资源预约项目说明与后续接口规划

> 文档对应分支：`codex/optimization-five-steps`  
> 最后验证：Docker/Testcontainers 全量测试 44/44 通过。

## 1. 项目是什么

这是一个基于 Java 21、Spring Boot 3.5、PostgreSQL 和 Redis 的实验室资源预约后端项目。

项目的核心目标是：用户登录后预约实验室可用时段；系统在并发请求较多时，仍能保证同一时段不会被重复预约，并能通过 Redis 队列削峰、缓存和状态跟踪维持稳定运行。

目前项目不是完整的前后端产品，而是以 REST API 为中心的后端服务。它提供了登录、自动预约最近可用时段、查询异步预约状态、取消预约等基础能力。

## 2. 技术组成与职责

| 技术/组件 | 用途 |
|---|---|
| Spring Boot | 应用启动、Web API、依赖注入、配置管理 |
| Spring MVC | Controller 层，对外提供 REST 接口 |
| Spring Security + JWT | 登录认证、无状态身份识别 |
| Spring Data JPA / Hibernate | 用户、时段、预约等数据访问 |
| PostgreSQL | 保存用户、可用时段、预约等最终业务数据 |
| Liquibase | 管理数据库表结构与约束变更 |
| Redis | 缓存、异步预约队列、请求状态、去重集合 |
| Bucket4j | 针对登录和预约接口的限流 |
| Micrometer + Actuator | 运行指标、健康检查、Prometheus 指标输出 |
| Docker Compose | 同时启动应用、PostgreSQL、Redis |
| Testcontainers | 在自动测试中启动真实 PostgreSQL 与 Redis 容器 |

## 3. 代码结构

```text
src/main/java/com/azki/reservation
├── config/        Spring、Redis、限流、指标、健康检查等配置
├── controller/    HTTP 接口入口
├── dto/           API 的请求和响应对象
├── entity/        User、AvailableSlot、Reservation 等数据库实体
├── repository/    JPA 查询与数据库锁查询
├── service/       预约、排队、过期清理、缓存等业务逻辑
├── security/      JWT 生成、解析及认证过滤器
├── filter/        限流、请求关联 ID 等 Web 过滤器
├── aspect/        通用日志、数据库日志、安全审计
└── exception/     业务异常与统一异常处理

src/main/resources
├── application.yml             主配置
├── application-dev.yml         开发环境配置
├── logback-spring.xml          日志格式与输出配置
└── db/changelog/               Liquibase 数据库迁移文件
```

## 4. 当前主要业务流程

### 4.1 登录流程

1. 客户端调用 `POST /api/auth/login`，提交邮箱和密码。
2. 服务端根据邮箱查询用户，并使用 BCrypt 校验密码。
3. 校验成功后签发 JWT。
4. 后续请求携带 `Authorization: Bearer <token>` 时，JWT 过滤器会识别用户身份。

### 4.2 自动预约流程

1. 客户端调用 `POST /api/v1/reservations/reserve`。
2. 系统判断当前处理压力。
3. 压力正常时，直接进入数据库预约流程；压力过高时，请求先写入 Redis 队列，立即返回 `202 Accepted` 和 `requestId`。
4. 预约写入流程在 PostgreSQL 中锁定一条可用时段。
5. 锁定成功后，将时段标记为已预约，并创建预约记录。
6. 如果同一用户已存在有效预约，或没有可用时段，则返回对应的业务错误。

### 4.3 异步队列流程

Redis 队列包含以下状态：

```text
等待队列 -> 处理中 -> 成功
                 └-> 失败后重试 -> 死信队列（达到重试上限）
```

队列消息被领取后，会记录在“处理中”集合中；如果应用异常停止、消息长期未完成，定时任务会把超时消息放回等待队列，避免消息丢失。

## 5. 当前已有接口

| 方法 | 路径 | 用途 |
|---|---|---|
| POST | `/api/auth/login` | 使用邮箱和密码登录，返回 JWT |
| POST | `/api/v1/reservations/reserve` | 自动预约最近的可用时段 |
| GET | `/api/v1/reservations/status/{requestId}` | 查询异步预约请求状态 |
| DELETE | `/api/v1/reservations/cancel/{id}` | 取消指定预约 |

### 现有接口的不足

当前预约接口仍允许请求体传入邮箱，且预约接口暂时允许匿名访问。这意味着恶意调用者理论上可以填写其他人的邮箱发起预约。因此，下一阶段最重要的工作是把预约相关接口改为必须登录，并从 JWT 中取得当前用户，而不是相信请求体中的邮箱。

## 6. 已完成的优化

### 6.1 并发预约与数据库完整性

原风险：多个请求同时读取同一个可用时段后，可能都尝试创建预约。

已完成改进：

- 写预约时不再使用缓存提供的时段，直接访问数据库。
- 使用 PostgreSQL 的 `FOR UPDATE SKIP LOCKED` 锁定候选时段。
- 已被其他事务锁定的时段会被跳过，避免请求长时间互相等待。
- 增加用户预约唯一约束，防止同一用户保留多条有效预约。
- 增加时段时间约束：结束时间必须晚于开始时间。
- 创建预约时使用 `saveAndFlush`，及时将数据库唯一约束冲突转为业务异常。
- 增加两个并发线程争抢预约的真实 PostgreSQL 集成测试。

相关文件：

- `repository/TimeSlotRepository.java`
- `service/ReservationService.java`
- `db/changelog/changeset-003-concurrency-constraints.xml`

### 6.2 预约过期规则

原逻辑按预约的创建时间判断过期。例如用户今天预约三天后的时段，24 小时后可能被错误删除。

已完成改进：

- 现在按预约时段的 `endTime` 判断是否过期。
- 只有时段已经结束，系统才释放时段并删除预约记录。
- 查询过期预约时同时加载关联时段，减少清理任务的额外查询。
- 默认每 15 分钟执行一次清理任务。

### 6.3 密钥与配置安全

原风险：JWT 密钥与数据库密码曾直接写在代码或 Docker 配置中。

已完成改进：

- JWT 密钥改为读取 `JWT_SECRET` 环境变量。
- 启动时检查 JWT 密钥至少为 32 个 UTF-8 字节。
- JWT 有效期可以用 `JWT_EXPIRATION_MS` 调整。
- 数据库密码改为通过 `DB_PASSWORD` 或 Docker 环境变量提供。
- 增加 `.env.example` 作为模板；真实 `.env` 已被 Git 忽略。
- Docker Compose 不再保存真实密码。
- 修复匿名请求审计人被错误记录为 `anonymousUser` 的问题，匿名/定时任务统一记录为 `system`。

### 6.4 限流能力

原风险：所有用户共用同一个限流桶，一个用户高频访问会影响所有用户；原路径也未匹配真实预约接口。

已完成改进：

- 限流范围修正为 `/api/v1/reservations/**` 和 `/api/auth/login`。
- 登录与预约使用不同的限流组。
- 每个客户端 IP 拥有独立令牌桶，互不占用额度。
- 默认每分钟允许 20 次请求，可在 `application.yml` 中调整。
- 超限返回 `429 Too Many Requests`、JSON 错误消息和 `Retry-After` 响应头。
- 限制内存中保存的客户端桶数量，防止恶意来源造成内存无限增长。

### 6.5 日志、指标与运行健康

已完成改进：

- 修复日志每条重复输出两次的问题。
- 通用日志切面不再输出完整参数与返回值，避免密码、令牌等敏感信息泄露到日志。
- 新增 `X-Request-ID`：每次 HTTP 请求都有唯一关联标识，响应头会回传该标识，日志也会包含它。
- 客户端传入不安全的请求 ID 时，服务端会自动替换，避免日志注入。
- 正式记录预约总耗时与时段锁定查询耗时。
- 队列长度、处理中数量、死信队列长度已经作为运行指标暴露。
- 队列健康检查阈值改为可配置；队列积压过多时 Actuator 会返回 `WARNING` 或 `DOWN`。

## 7. 数据库与部署注意事项

### 7.1 数据库迁移

项目使用 Liquibase 管理数据库结构。启动应用时会自动执行未执行的变更集。

新增的用户预约唯一约束在已有数据库中可能遇到问题：如果数据库已经存在同一用户的多条预约，需要先清理重复数据，再执行迁移。

### 7.2 Docker 启动前准备

先复制环境变量模板：

```bash
cp .env.example .env
```

然后修改 `.env` 中的内容：

```text
POSTGRES_PASSWORD=你的数据库密码
JWT_SECRET=至少32字节的随机密钥
```

如果以前已经用 Docker 启动过 PostgreSQL，原数据卷中的数据库账号仍使用旧密码。此时应继续在 `.env` 中填写旧密码，或者在确认数据可丢弃后重建数据卷。

## 8. 建议新增的接口

以下接口均为建议，当前尚未实现。

### 8.1 第一优先级：用户身份与预约闭环

#### 1）查询我的预约

```text
GET /api/v1/me/reservations
```

用途：查询当前登录用户自己的预约记录。

建议参数：

```text
page=0
size=20
status=ACTIVE / CANCELLED / EXPIRED
```

建议响应：

```json
{
  "content": [
    {
      "id": 12,
      "status": "ACTIVE",
      "reservedAt": "2026-09-16T10:00:00",
      "slot": {
        "id": 8,
        "startTime": "2026-09-17T09:00:00",
        "endTime": "2026-09-17T10:00:00"
      }
    }
  ],
  "page": 0,
  "size": 20,
  "totalElements": 1
}
```

规则：用户身份必须从 JWT 中获取，不允许客户端传入任意邮箱。

#### 2）查询单条预约详情

```text
GET /api/v1/reservations/{id}
```

用途：查看一条预约的完整信息。

规则：普通用户只能查询自己的预约；管理员可查询任意预约；不存在返回 `404`，无权限返回 `403`。

#### 3）调整现有自动预约接口

当前接口：

```text
POST /api/v1/reservations/reserve
```

建议改造：不再从请求体接收 `email`，直接读取 JWT 身份。未来若有多个实验室、资源类型，可提交资源筛选条件：

```json
{
  "resourceType": "LAB",
  "locationId": 1
}
```

#### 4）调整取消预约接口

建议将路径改为标准 REST 风格：

```text
DELETE /api/v1/reservations/{id}
```

规则：用户只能取消自己的、尚未开始的预约；管理员可以按权限取消；成功返回 `204 No Content`。

### 8.2 第二优先级：时段查询与指定预约

#### 1）查询可用时段

```text
GET /api/v1/slots
```

建议参数：

```text
start=2026-09-17T00:00:00
end=2026-09-18T00:00:00
resourceType=LAB
page=0
size=20
```

建议响应：

```json
{
  "content": [
    {
      "id": 8,
      "startTime": "2026-09-17T09:00:00",
      "endTime": "2026-09-17T10:00:00",
      "available": true
    }
  ]
}
```

只返回尚未开始且未被预约的时段；不要直接返回数据库实体的密码、审计字段等内部信息。

#### 2）指定时段创建预约

```text
POST /api/v1/reservations
```

请求：

```json
{
  "slotId": 8
}
```

建议响应：

```json
{
  "id": 12,
  "status": "ACTIVE",
  "slotId": 8,
  "startTime": "2026-09-17T09:00:00",
  "endTime": "2026-09-17T10:00:00"
}
```

实现时必须继续使用数据库行锁；客户端传了 `slotId` 不代表可以跳过并发校验。

### 8.3 第三优先级：认证与个人资料

#### 1）用户注册

```text
POST /api/auth/register
```

请求：

```json
{
  "email": "user@example.com",
  "userName": "张三",
  "password": "用户输入的密码"
}
```

规则：邮箱唯一；密码必须 BCrypt 加密后保存；密码不能出现在日志和响应中。

如果系统仅供内部人员使用，更适合由管理员创建账号，而不是开放注册。

#### 2）刷新令牌

```text
POST /api/auth/refresh
```

用途：使用 Refresh Token 获取新的短期访问令牌。实现前需要设计 Refresh Token 的保存位置、过期时间、撤销策略，推荐使用 Redis 或数据库。

#### 3）退出登录

```text
POST /api/auth/logout
```

用途：使 Refresh Token 失效。纯 JWT 无法主动撤销，因此该接口必须与 Refresh Token 或 JWT 黑名单一起实现。

#### 4）查看个人资料

```text
GET /api/v1/me
```

建议响应：

```json
{
  "id": 1,
  "email": "user@example.com",
  "userName": "张三"
}
```

### 8.4 第四优先级：管理员接口

管理员接口需要先在用户表中引入角色，例如 `USER`、`ADMIN`，并限制 `/api/v1/admin/**` 只能由管理员调用。

#### 时段管理

| 方法 | 路径 | 作用 |
|---|---|---|
| POST | `/api/v1/admin/slots` | 创建单个或批量可用时段 |
| GET | `/api/v1/admin/slots` | 按日期、状态查询全部时段 |
| PUT | `/api/v1/admin/slots/{id}` | 修改尚未被预约的时段 |
| DELETE | `/api/v1/admin/slots/{id}` | 删除尚未被预约的时段 |

批量创建时段的请求示例：

```json
{
  "startDate": "2026-09-17",
  "endDate": "2026-09-21",
  "dailyStartTime": "09:00",
  "dailyEndTime": "18:00",
  "durationMinutes": 60
}
```

#### 预约与队列管理

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/api/v1/admin/reservations` | 分页查询全部预约，可按用户、日期、状态筛选 |
| DELETE | `/api/v1/admin/reservations/{id}` | 管理员强制取消预约 |
| GET | `/api/v1/admin/queue` | 查询等待、处理中、死信队列数量 |
| GET | `/api/v1/admin/queue/dlq` | 查看死信队列中的失败请求 |
| POST | `/api/v1/admin/queue/dlq/{requestId}/retry` | 人工重试指定死信请求 |

死信队列接口只能由管理员调用，不能暴露给普通用户。

## 9. 推荐后续实施顺序

1. 给用户增加角色并收紧安全规则，预约接口必须登录。
2. 增加“我的预约”和“预约详情”接口。
3. 改造自动预约与取消接口，使其从 JWT 获取当前用户。
4. 增加可用时段查询和指定时段预约。
5. 增加管理员时段管理、预约管理。
6. 增加注册、刷新令牌、退出登录。
7. 根据实际运营需要增加死信队列人工重试和后台管理页面。

## 10. 当前状态总结

项目已经具备较好的基础稳定性：数据库并发保护、Redis 队列可靠性、密码与密钥外部化、客户端限流、日志关联、指标和容器化测试均已完成。

下一阶段的重点不应再只是“增加一个接口”，而应首先完成身份与权限闭环。只有让预约行为与 JWT 当前用户绑定，并区分普通用户和管理员，后续的预约查询、时段管理和后台管理接口才会安全、清晰、可维护。
